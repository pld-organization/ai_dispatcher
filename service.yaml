apiVersion: v1
kind: Service
metadata:
  name: cancer-dispatcher-api
spec:
  selector:
    app: cancer-dispatcher-api
  type: NodePort
  ports:
    - port: 8100
      targetPort: 8100
      nodePort: 32132